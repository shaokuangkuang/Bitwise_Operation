#include <stdio.h>
#include <stdlib.h>
//&(位“与”)，^(位“异或”)，|（位“或”），~（位“取反”）
//移位运算符：<<(左移)，>>(右移)
void main()
{
	unsigned char ch = 0;
	printf("%d\n", sizeof(ch));
	printf("%p\n", &ch);
	//可以通过赋值操作二进制
	system("pause");

}